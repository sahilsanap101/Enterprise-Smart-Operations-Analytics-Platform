server:
  port: 8080

spring:
  application:
    name: enterprise-smart-ops

  datasource:
    url: jdbc:postgresql://localhost:5432/enterprise_smart_ops
    username: postgres
    password: Sahil@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: enterprise-smart-ops-secret-key-1234567890123456
  expiration: 36000000
